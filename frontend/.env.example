# SEC60 Frontend — Backend API URL
#
# Production (Vercel):
#   Set this in Vercel → Project → Settings → Environment Variables
#   Then trigger a new deployment so the value is baked into the build.
VITE_API_BASE_URL=https://sec60.onrender.com

# Local development (override in frontend/.env — never commit that file):
# VITE_API_BASE_URL=http://localhost:8000
